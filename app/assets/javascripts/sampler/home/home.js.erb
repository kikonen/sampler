//= depend_on_asset "home/home.html"
"use strict";
angular
  .module('sampler.home', [
    'ui.router'])
  .config(function($stateProvider) {
    $stateProvider
      .state({
        name: "root.home",
        url: "/home",
        templateUrl: "<%= asset_path('home/home.html') %>",
        controller: 'sampler.home'
      });
  })
  .controller(
    'sampler.home',
    function($scope) {
      $scope.data = {
        message: 'koti'
      };
    });
